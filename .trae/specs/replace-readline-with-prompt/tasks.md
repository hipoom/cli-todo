# Tasks

- [x] Task 1: 替换 template.kt 中的 readString
  - [x] 将 `readString("请输入你要添加的事项内容")` 替换为 `readLineWithPrompt("请输入你要添加的事项内容")`
  - [x] 移除 `import com.hipoom.cli.scaffold.utils.readString` 导入

- [x] Task 2: 替换 ColorPicker.kt 中的 readString
  - [x] 将 `readString("请输入您要选择的颜色代码")` 替换为 `readLineWithPrompt("请输入您要选择的颜色代码")`
  - [x] 移除 `import com.hipoom.cli.scaffold.utils.readString` 导入

- [x] Task 3: 替换 set.kt 中的 reader.readLine
  - [x] 将 `reader.readLine("请输入要设置的方案序号:")` 替换为 `readLineWithPrompt("请输入要设置的方案序号:")`

- [x] Task 4: 替换 TodoShellHandler.kt 中的 readString
  - [x] 将 `readString("请选择是否继续执行该指令[yes/no]")` 替换为 `readLineWithPrompt("请选择是否继续执行该指令[yes/no]")`
  - [x] 移除 `import com.hipoom.cli.scaffold.utils.readString` 导入

- [x] Task 5: 替换 TemplateHandler.kt 中的 readString
  - [x] 将 `readString("请输入模板名称")` 替换为 `readLineWithPrompt("请输入模板名称")`
  - [x] 移除 `import com.hipoom.cli.scaffold.utils.readString` 导入

- [x] Task 6: 替换 CommentHandler.kt 中的 readString
  - [x] 将 `readString("请输入要添加的备注")` 替换为 `readLineWithPrompt("请输入要添加的备注")`
  - [x] 移除 `import com.hipoom.cli.scaffold.utils.readString` 导入

# Task Dependencies
- 所有任务相互独立，可并行执行
